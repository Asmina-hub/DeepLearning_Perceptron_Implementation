"""description of comments used
>>touch filename--to start a new empty file
>>conda create -n envname python=3.7-- creates the env in c drive
>>conda create --prefix ./env python=3.7 -y----- create env in the current drive
If conda is not present follow methods to get conda in git bash
1.got to anaconda folder>anaconda64>etc>profile.d>conda.csh
2.open gitbash in that folder and type
echo ".{PWD}/conda.sh" >> ~ /.bashrc
try conda init
3.then type conda activate
this will activate your conda
>>conda activate ./env


"""
